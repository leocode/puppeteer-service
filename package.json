{
  "name": "puppeteer-service",
  "version": "1.0.0",
  "scripts": {
    "start": "docker run --rm -it -p 3000:3000 $(docker build -q .)"
  },
  "dependencies": {
    "fastify": "^3.14.0",
    "puppeteer-core": "^8.0.0"
  }
}
